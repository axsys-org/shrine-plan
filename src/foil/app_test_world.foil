-  sept [order=10]
-  shrine_types [order=20]
-  bond [order=30]
-  shrine [order=40]
-  lain_types [order=50]
-  lain_peek [order=60]
-  lain_poke [order=70]
-  weft [order=80]
-  tyshow [order=90]
-  pact [order=100]
-  http [order=110]
-  web [order=120]
-  helm [order=130]
-  helm_actor [order=140]
-  helm_http [order=150]
-  eden [order=160]
-  testing [order=170]
'  Direct application tests use real kernel transitions, without actors,
'  sockets, compiler mirrors, or a filesystem-wide boot.
+  app_world
  : state=helm valid=bool
  +  step
    \ command=task world=app_world
    ^ app_world
    = [next duties result spill issues]
        | helm/exec .none command world.state
    = okay=bool
        ? result
         > (./ok _) (? ?=(.none spill) !(issues.len) .false)
        .false
    | app_world next (? okay world.valid .false)
  +  refuses
    '  Only a driving-event nack counts as refusal.
    \ command=task world=app_world
    ^ bool
    = [next duties result spill issues]
        | helm/exec .none command world.state
    ?=((./no _) result)
  +  make
    \ target=path record=myth world=app_world
    ^ app_world
    | app_world/step (task/vine target (writ/make record)) world
  +  poke
    \ target=path record=myth world=app_world
    ^ app_world
    | app_world/step (task/vine target (writ/poke record)) world
  +  get
    \ target=path world=app_world
    ^ myth
    ? (x_query_h world.state.sov.top target world.state.sov)
     > (./known record) record
    myth/new
  +  tree
    \ target=path world=app_world
    ^ axal[myth]
    ? world.state.sov.z_latest(target)
     > (./known tree) tree
    (axal/new[myth] .none)
  +  number
    \ target=path slot=path fallback=nat world=app_world
    ^ nat
    (app_world/get target world).nat_at(slot fallback)
  +  text
    \ target=path slot=path world=app_world
    ^ str
    (app_world/get target world).txt_at(slot)
  +  pail
    \ target=path slot=path world=app_world
    ^ maybe[pail]
    (app_world/get target world).get(slot)
  +  exists
    \ target=path world=app_world
    ^ bool
    ?=((./known _) (x_query_h world.state.sov.top target
      world.state.sov))
  +  seed
    ^ app_world
    = [state duties] | helm/boot sov/new
    = base | app_world state .true
    = base
        | app_world/step eden/second
        | app_world/step eden/first base
    | app_world/make ['kook 'pact]
      (form/card pact/form (lore/line "Application mounts.")) base
  +  mount
    \ manifest=mani name=text root=path world=app_world
    ^ app_world
    = next | app_world/make root (pact/card manifest name 0 0) world
    | (zoo/tasks root name manifest).fold next
    \ current command
    | app_world/step command current
  +  post
    \ target=path fields=row[[key=iota value=text]] world=app_world
    ^ app_world
    = record
        | fields.fold myth/new
        \ record [key value]
        record.put(['post key] (pails/t value))
    | app_world/poke target record world
  +  page
    \ target=path fragment=bool world=app_world
    ^ text
    = record | http/req "GET" "" "" target
      record ? fragment record.put(['frag] (pd 1)) record
      next | app_world/make ['request] record world
      next | app_world/poke ['request] (kooked [['kook 'hex]]) next
      response | app_world/get ['request] next
    ? !(next.valid) ""
    ? !(eq 200 response.nat_at(['res_status] 0)) ""
    | str/from_atom | response.nat_at ['res_body] 0
  +  contains
    \ expected=row[text] body=text
    ^ row[testing/check]
    | expected.map
    \ text
    | testing/nat text 1 (cord/has text body)
  +  lacks
    \ absent=row[text] body=text
    ^ row[testing/check]
    = checks
        | absent.map
        \ text
        | testing/nat text 0 (cord/has text body)
    | checks.snoc
      (testing/nat "successful nonempty response" 1 !(eq body ""))
  +  okay
    \ world=app_world
    ^ testing/check
    | testing/nat "all transitions committed" 1 world.valid
