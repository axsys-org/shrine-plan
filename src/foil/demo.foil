+  add
  \  a=nat b=nat
  ^  nat
  ?  (eq a 0)
     b
  (~ (dec a) (inc b))
+  point
  $  [x=nat y=nat]
  +  add
    \  l=point r=point
    (point (add l.x r.x) (add l.y r.y))
+  circle
  $  [center=point radius=nat]
  +  area
   \  c=circle
   ^  nat
   (mul 3 (mul c.radius c.radius))
+  geometry
  &  circle
     point
  +  area
   \  g=geometry
   ^  nat
   ?  g
    > point 0
    > c=circle c.area
+  fst
  (point 3 4)
+  snd
  (point 5 7)
+  geo
  ^  geometry
  fst
+  getx
  fst.x
+  same_as_getx
  (point/x fst)
+  maybe_nat
  &  ($some nat)
     $none
  +  map
    \  fun={nat; nat} x=maybe_nat
    ^  nat
    ?  x
     > ($some a=nat) (fun a)
     > $none         0
  +  or_default
    \  default=nat x=maybe_nat
    ^  nat
    ?  x
     > ($some a=nat) a
     > $none  default
+  test_maybe_nat
  ^  maybe_nat
  ($some 3)
+  test_maybe_nat_missing
  ^  maybe_nat
  $none
+  test_maybe_inc
  (maybe_nat/map inc test_maybe_nat)
+  test_add_ufcs
  test_maybe_nat.map((add 3))
+  test_or_default
  test_maybe_nat_missing.or_default(5)
