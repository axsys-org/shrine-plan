'  Supported Grove backend fixture: one record per declaration.

token =
  @type
  ! foil
  : value=nat

counter =
  @slot
  #meta ;
    "/0x11/vocabulary/title": "Counter"
    "/0x11/vocabulary/count": (pails/n (counter/read (counter/make 7)))
  ! foil
  + make
    \ value=nat
    ^ token
    | token value
  + read
    \ value=token
    ^ nat
    value.value

label =
  @slot
  #meta ;
    "/0x11/vocabulary/title": "Documentation only"
