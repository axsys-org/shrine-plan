;; ok = success, no = failure, s = state, v = value

(define (tr-id ok no s v)
    (ok s v))

(define (tr-empty ok no s v)
    (no s v))

(define (tr-compose-cont g ok no ns nv)
  (g ok no ns nv))

(define (tr-compose f g ok no s v)
  (f (tr-compose-cont g ok no) no s v))