{
  "title": "ev",
  "type": "object",
  "required": ["body"],
  "properties": {"body": {"oneOf": [{"$ref": "#/$defs/multi"}, {"$ref": "#/$defs/other"}]}},
  "$defs": {
    "multi": {"type": "object", "required": ["kind"], "properties": {"kind": {"enum": ["a", "b"]}}},
    "other": {"type": "object", "required": ["kind"], "properties": {"kind": {"type": "string"}}}
  }
}
