{
  "title": "tail",
  "type": "object",
  "properties": {
    "ttl": {"enum": ["5m", "1h"]},
    "status": {"type": "integer", "enum": [0, -1, -2]},
    "engine": {"anyOf": [{"enum": ["a", "b", "c"]}, {"enum": ["d"]}]}
  }
}
