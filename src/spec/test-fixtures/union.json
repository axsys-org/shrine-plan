{
  "title": "evt",
  "type": "object",
  "required": ["body"],
  "properties": {"body": {"oneOf": [{"$ref": "#/$defs/a"}, {"$ref": "#/$defs/b"}]}},
  "$defs": {
    "a": {
      "type": "object",
      "required": ["kind", "n"],
      "properties": {"kind": {"enum": ["alpha"]}, "n": {"type": "integer"}}
    },
    "b": {
      "type": "object",
      "required": ["kind", "s"],
      "properties": {"kind": {"enum": ["beta"]}, "s": {"type": "string"}}
    }
  }
}
