{
  "title": "doc",
  "type": "object",
  "properties": {"callers": {"$ref": "#/$defs/callers"}, "shots": {"$ref": "#/$defs/shots"}},
  "$defs": {
    "callers": {"type": "array", "items": {"type": "string", "enum": ["direct", "relayed"]}},
    "shots": {
      "type": "array",
      "items": {"type": "object", "required": ["url"], "properties": {"url": {"type": "string"}}}
    }
  }
}
