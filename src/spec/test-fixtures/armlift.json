{
  "title": "inp",
  "type": "object",
  "properties": {
    "body": {
      "anyOf": [{"type": "string"}, {"items": {"oneOf": [{"$ref": "#/$defs/a"}, {"$ref": "#/$defs/b"}]}}]
    }
  },
  "$defs": {
    "a": {"type": "object", "required": ["kind"], "properties": {"kind": {"enum": ["alpha"]}}},
    "b": {"type": "object", "required": ["kind"], "properties": {"kind": {"enum": ["beta"]}}}
  }
}
