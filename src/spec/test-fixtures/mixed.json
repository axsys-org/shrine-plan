{
  "title": "mix",
  "type": "object",
  "properties": {
    "kind": {"enum": ["fixed"]},
    "hue": {"enum": ["Red", "Green"]},
    "side": {"anyOf": [{"enum": ["L"]}, {"enum": ["R"]}]},
    "blob": {}
  }
}
