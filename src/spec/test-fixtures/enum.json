{
  "title": "box",
  "type": "object",
  "required": ["tag"],
  "properties": {"tag": {"$ref": "#/$defs/grp"}, "opt": {"$ref": "#/$defs/grp"}},
  "$defs": {"grp": {"enum": ["Qwen", "Claude", null]}}
}
