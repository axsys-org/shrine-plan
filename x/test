#!/bin/sh
# Compatibility entry point. --reset now requests a fresh staged cache.
if [ "${1:-}" = --reset ]; then
  shift
  exec "$(dirname "$0")/check" --fresh "$@"
fi
exec "$(dirname "$0")/check" "$@"
